# 基于RAG技术的经典人工智能算法智能辅导与错误诊断辅助学习系统的概要设计说明书

**项目名称** 基于RAG技术的经典人工智能算法智能辅导与错误诊断辅助学习系统

**文档状态** []草稿文档

**当前版本** v1.0.0

**作者** PasteK0n

**完成日期** 

**版权所有** PasteK0n@github.com

---

# 修改历史

| 日期       | 版本   | 作者           | 修改内容                                                     | 评审号 | 更改请求号 |
| :--------- | :----- | :------------- | :----------------------------------------------------------- | :----- | :--------- |
| 2026-08-06 | V1.0.0 | PasteK0n       | 基本框架制定与内容撰写                             | R-001  | CR-001       |

---

## 目录

- [修改历史](#修改历史)
- [一、 引言 (Introduction)](#一-引言-introduction)
  - [1.1 编写目的](#11-编写目的)
  - [1.2 文档范围](#12-文档范围)
  - [1.3 参考资料](#13-参考资料)
- [二、系统总体设计 (System Total Design)](#二-系统总体设计-system-total-design)
  - [2.1 整体架构](#21-整体架构)
  - [2.2 整体功能架构](#22-整体功能架构)
  - [2.3 整体技术架构](#23-整体技术架构)
  - [2.4 运行环境设计](#24-运行环境设计)
  - [2.5 设计目标](#25-设计目标)
- [三、系统功能模块设计 (System Function Module Design)](#三-系统功能模块设计-system-function-module-design)
  - [3.1 个人办公](#31-个人办公)
  - [3.2 系统管理](#32-系统管理)
- [四、性能设计 (Performance Design)](#四-性能设计-performance-design)
- [五、接口设计 (API Design)](#五-接口设计-API-design)
  -[5.1 接口设计原则](#51-接口设计原则)
  -[5.2 接口实现方式](#52-接口实现方式)
- [六、数据库设计 (Database Design)](#六-数据库设计-database-design)
- [七、出错处理与可靠性设计 (Reliability & Fault-Tolerance)](#七-出错处理与可靠性设计-reliability-&-fault-tolerance)

---
## 一、引言
### 1.1 编写目的

本文档是“基于RAG技术的经典人工智能算法与错误诊断辅助学习系统”的概要设计说明书。编写文档的目的在于：

1. 设计系统整体架构，为开发者提供简洁明了的系统构成；
2. 设计系统功能模块、接口与数据库，达成目标功能；
3. 进行出错处理与可靠性设计，提高系统稳定性。

### 1.2 文档范围

本文档涵盖该AI搭建系统的构建过程，包括：

* **引言与总体设计**：项目设立初衷和系统整体架构设计
* **功能模块与性能设计**：不同用户界面包含功能，以及系统需要达到性能标准
* **接口、数据库与可靠性设计**：设计前后端交互接口、基于RAG的人工智能算法数据库，以及增强系统鲁棒性

### 1.3 参考资料

1.  GB/T 8567-2006《计算机软件文档编制规范》，中华人民共和国国家质量监督检验检疫总局，2006年
2.  ChromaDB 高维向量检索持久化规范：https://docs.trychroma.com/
3.  《Retrieval-Augmented Generation for Large Language Models: A Survey》, Tongxiang Fan, et al., 2023.

## 二、系统整体设计
### 2.1 整体架构

本系统面向个人自主学习与辅助，采用“前端 Web + 本地后端 + 本地/云端混合大模型”的架构。系统通过 FastAPI 整合 RAG（检索增强生成） 知识库与 代码作业辅助 Agent，并结合 Prompt 动态引擎 实现全局能力迭代与个人学习偏好记忆的实时合成。

```mermaid
%%{init: { "flowchart": { "curve": "step"}}}%%
flowchart TD
    %% 纯黑白高对比度样式定义
    classDef blackWhite fill:#ffffff,stroke:#000000,stroke-width:2px,color:#000000;
    
    %% 设定所有连线和箭头的颜色为纯黑，线宽加粗至 2px
    linkStyle default stroke:#000000,stroke-width:2px;

    %% ==================== 1. 展示层 (Web 客户端) ====================
    subgraph Client ["前端展示层 (Web)"]
        Vue["Web 客户端 (Vue 3)<br>· Markdown & 数学公式渲染<br>· 代码高亮 & SSE 流式输出"]
    end

    %% ==================== 2. 后端核心路由与业务层 ====================
    subgraph Backend ["后端业务控制层 (FastAPI)"]
        FastAPI["FastAPI 聚合入口"]
        JWT["JWT 鉴权<br>(用户 / 管理员)"]
        ORM["SQLAlchemy ORM<br>(数据持久化接口)"]
        PromptEngine["Prompt 动态引擎<br>· 个人 Memory 实时拼接<br>· 反馈关键词切面捕获"]
    end

    %% ==================== 3. 异步任务与数据持久化层 ====================
    subgraph StorageLayer ["数据持久化与异步计算层"]
        Celery["Celery Worker<br>· 文档离线向量化<br>· Prompt 闭环演化分析"]
        MySQL["MySQL 主库<br>· 账号/权限<br>· 个人 Memory 表<br>· Prompt 版本与反馈日志"]
        ChromaDB["ChromaDB<br>(算法知识库向量检索)"]
        SQLite["SQLite FTS5<br>(关键词 BM25 全文检索)"]
    end

    %% ==================== 4. RAG 解析与知识检索层 ====================
    subgraph RAGModule ["RAG 知识库解析与检索模块"]
        RAG["RAG 检索与解析引擎<br>· PyMuPDF4LLM 文本提取<br>· PaddleOCR 公式/图表识别"]
    end

    %% ==================== 5. 模型执行网关层 ====================
    subgraph ModelGateway ["模型路由网关 (Model Gateway)"]
        LocalModel["本地模型服务<br>(Ollama / vLLM)<br>DeepSeek-R1 / Qwen2.5"]
        CloudAPI["云端 API 服务<br>(OpenAI / Claude / DeepSeek)"]
    end

    %% ==================== 核心数据流向与调用关系 ====================
    %% 前后端通信
    Vue -->|"HTTP (用户/管理员管理路由)"| FastAPI
    Vue -->|"SSE (算法问答/代码 Debug 流式数据)"| FastAPI

    %% 后端内部组件协同
    FastAPI -->|"角色鉴权与校验"| JWT
    FastAPI -->|"CRUD / 存取个人 Memory"| ORM
    FastAPI -->|"实时动态组装 System Prompt"| PromptEngine
    FastAPI -->|"离线解析 / 定时迭代任务派发"| Celery

    %% Prompt 引擎与持久化/网关交互
    PromptEngine -->|"读取全局模板与用户 Memory"| ORM
    PromptEngine -->|"捕获负面关键词并记录日志"| ORM
    PromptEngine -->|"输出组装好的 System Prompt"| LocalModel
    PromptEngine -->|"输出组装好的 System Prompt"| CloudAPI

    %% 持久化数据流转
    ORM --> MySQL
    Celery --> RAG
    Celery -->|"离线分析 Bad Case，更新全局 Prompt"| ORM

    %% RAG 检索流转
    FastAPI -->|"发起知识库检索"| RAG
    RAG --> ChromaDB
    RAG --> SQLite
    RAG -->|"注入相关 Context 上下文"| LocalModel
    RAG -->|"注入相关 Context 上下文"| CloudAPI

    %% 统一应用纯黑白高清晰度样式
    class Vue,FastAPI,JWT,ORM,PromptEngine,Celery,MySQL,ChromaDB,SQLite,RAG,LocalModel,CloudAPI blackWhite;
```

### 2.2 整体功能架构

本系统实现以下功能：
1. 搭建基于RAG的人工智能经典算法数据库
2. Python代码辅助
3. Prompt动态合成与演化
4. Agent搭建与web界面前端展示

```mermaid
%%{init: { "flowchart": { "curve": "step"}}}%%
flowchart TD
    %% 纯黑白高对比度样式定义
    classDef blackWhite fill:#ffffff,stroke:#000000,stroke-width:2px,color:#000000;
    
    %% 设定所有连线和箭头的颜色为纯黑，线宽加粗至 2px
    linkStyle default stroke:#000000,stroke-width:2px;

    %% ==================== 1. 展示层 (User Interface) ====================
    subgraph UI ["展示层 (Vue 3 Web 客户端)"]
        ChatUI["算法问答与对话界面<br>(SSE 流式响应 / Markdown & LaTeX 渲染)"]
        DebugUI["代码作业 Debug 工作台<br>(代码高亮 / 错误定位 / 分步引导)"]
        KbUI["知识库管理界面<br>(算法 PDF 导入 / 向量化进度监控)"]
        AdminUI["系统管理控制台<br>(Prompt 版本审核 / 负面关键词日志)"]
    end

    %% ==================== 2. 应用功能层 (Application Layer) ====================
    subgraph AppLayer ["应用功能层 (FastAPI 业务路由)"]
        AuthModule["用户与权限模块<br>(JWT 鉴权 / 角色控制: 用户与管理员)"]
        RagService["算法知识库服务<br>(算法概念问答 / 步骤拆解 / 示例代码生成)"]
        CodeService["代码作业辅助服务<br>(语法/逻辑诊断 / 报错分析 / 启发式引导)"]
        PromptService["Prompt 管理服务<br>(版本切换 / 反馈关键词收集 / 个人 Memory)"]
    end

    %% ==================== 3. Agent 核心引擎层 (Core Engines) ====================
    subgraph EngineLayer ["Agent 核心引擎层"]
        PromptEngine["Prompt 动态引擎<br>· 全局模板与个人 Memory 拼接<br>· 负面/纠错关键词切面捕获"]
        RagEngine["RAG 混合检索引擎<br>· PyMuPDF4LLM 文本提取<br>· PaddleOCR 图表/公式识别<br>· 向量 + BM25 混合检索"]
        EvalEngine["Celery 离线演进引擎<br>· Bad Case 归因分析<br>· 高频关键词聚类<br>· 全局 Prompt vNext 拟定"]
    end

    %% ==================== 4. 数据与存储层 (Data Layer) ====================
    subgraph DataLayer ["数据与存储层"]
        MySQL["MySQL 主数据库<br>· 用户账号 & 权限<br>· 个人 Memory 偏好<br>· System Prompt 版本表<br>· 用户反馈关键词日志"]
        ChromaDB["ChromaDB 向量数据库<br>(算法切片向量 Embedding)"]
        SQLite["SQLite FTS5 数据库<br>(算法术语 BM25 全文索引)"]
    end

    %% ==================== 5. 基础设施与模型层 (Infrastructure & LLM) ====================
    subgraph InfraLayer ["基础设施与模型层"]
        Gateway["模型路由网关 (Model Gateway)"]
        LocalLLM["本地模型服务 (Ollama / vLLM)<br>(DeepSeek-R1 / Qwen2.5)"]
        CloudLLM["云端模型 API 服务<br>(OpenAI / Claude / DeepSeek)"]
    end

    %% ==================== 功能流向与依赖关系 ====================
    %% 展示层 -> 应用层
    ChatUI --> AppLayer
    DebugUI --> AppLayer
    KbUI --> AppLayer
    AdminUI --> AppLayer

    %% 应用层 -> 核心引擎层
    AuthModule --> PromptEngine
    RagService --> RagEngine
    RagService --> PromptEngine
    CodeService --> PromptEngine
    PromptService --> PromptEngine
    PromptService --> EvalEngine

    %% 核心引擎层 -> 数据层
    PromptEngine --> MySQL
    RagEngine --> ChromaDB
    RagEngine --> SQLite
    EvalEngine --> MySQL

    %% 核心引擎层 -> 基础设施层
    PromptEngine --> Gateway
    RagEngine --> Gateway
    Gateway --> LocalLLM
    Gateway --> CloudLLM

    %% 统一应用纯黑白高清晰度样式
    class ChatUI,DebugUI,KbUI,AdminUI,AuthModule,RagService,CodeService,PromptService,PromptEngine,RagEngine,EvalEngine,MySQL,ChromaDB,SQLite,Gateway,LocalLLM,CloudLLM blackWhite;
```

### 2.3 整体技术架构

本系统前端采用 Vue 3 + Vite + Tailwind CSS 搭配 SSE 实现响应式的 Markdown/LaTeX 渲染与代码流式输出；后端基于 FastAPI 搭建高并发路由并集成 JWT 权限控制，采用 Jinja2 实现“全局 Prompt 模板 + 用户 Memory”的动态合成；数据持久化由 MySQL + SQLAlchemy ORM 承载，配合 Redis + Celery 处理文档异步向量化与离线 Prompt 闭环演化分析；知识库检索整合 PyMuPDF4LLM + PaddleOCR 进行文档解析，采用 ChromaDB（向量检索）+ SQLite FTS5（BM25 全文检索） 的混合检索架构；模型层通过 Model Gateway 实现了对本地 Ollama（如 DeepSeek-R1 / Qwen2.5） 与外部云端 API 的无缝切换与统一调度。

```mermaid
%%{init: { "flowchart": { "curve": "step"}}}%%
flowchart TD
    %% 纯黑白高对比度样式定义
    classDef blackWhite fill:#ffffff,stroke:#000000,stroke-width:2px,color:#000000;
    
    %% 设定所有连线和箭头的颜色为纯黑，线宽加粗至 2px
    linkStyle default stroke:#000000,stroke-width:2px;

    %% ==================== 1. 前端技术栈 (Frontend Stack) ====================
    subgraph FrontendTech ["前端技术栈 (Web)"]
        Vue3["Vue 3 (Composition API)"]
        Vite["Vite 构建工具"]
        Tailwind["Tailwind CSS 样式库"]
        Monaco["Monaco Editor (代码编辑器)"]
        Katex["KaTeX & Markdown-it (公式/文档渲染)"]
    end

    %% ==================== 2. 后端技术栈 (Backend Stack) ====================
    subgraph BackendTech ["后端业务与框架层 (FastAPI)"]
        FastAPIApp["FastAPI 异步 Web 框架"]
        PyJWT["PyJWT (JWT RBAC 鉴权切面)"]
        Jinja2["Jinja2 (System Prompt 动态渲染)"]
        AsyncIO["AsyncIO + EventSource (SSE 流式输出)"]
        SQLAlchemyORM["SQLAlchemy 2.0 ORM"]
    end

    %% ==================== 3. AI & Agent 核心组件 (Agent Components) ====================
    subgraph AgentTech ["AI & Agent 核心引擎框架"]
        PromptDynamic["PromptEngine (内存实时合成 + 关键词捕获)"]
        RAGPipe["RAG Pipeline (PyMuPDF4LLM + PaddleOCR)"]
        EmbeddingEngine["Sentence-Transformers (向量嵌入引擎)"]
        LangChainGate["Model Gateway (LiteLLM / OpenAI SDK 统一分发)"]
    end

    %% ==================== 4. 数据与存储中间件 (Data & Middleware) ====================
    subgraph DataStack ["数据与存储中间件层"]
        MySQLDb["MySQL 8.0 (主数据库)"]
        RedisCache["Redis (Prompt 模板与 Hot-Memory 缓存)"]
        ChromaStore["ChromaDB (向量数据库)"]
        SQLiteFTS["SQLite 3 (FTS5 BM25 全文检索)"]
        CeleryMQ["Celery + Redis (异步/定时任务队列)"]
    end

    %% ==================== 5. 基础设施与模型推理 (Infra & Inference) ====================
    subgraph InfraTech ["基础设施与模型推理层"]
        LocalInference["本地推理引擎 (Ollama / vLLM)<br>· DeepSeek-R1<br>· Qwen2.5-Coder"]
        CloudInference["云端 API 网关<br>· OpenAI API<br>· DeepSeek Cloud API"]
    end

    %% ==================== 技术组件流向与依赖关系 ====================
    %% 前端内部整合
    Vue3 --- Vite
    Vue3 --- Tailwind
    Vue3 --- Monaco
    Vue3 --- Katex

    %% 前后端网络通信
    Vue3 -->|"HTTP / REST API"| FastAPIApp
    Vue3 -->|"EventSource (SSE Stream)"| AsyncIO

    %% 后端内部组件与切面
    FastAPIApp --> PyJWT
    FastAPIApp --> Jinja2
    FastAPIApp --> SQLAlchemyORM
    FastAPIApp --> CeleryMQ

    %% 后端调用 Agent 核心引擎
    Jinja2 --> PromptDynamic
    FastAPIApp --> RAGPipe
    FastAPIApp --> LangChainGate

    %% Agent 引擎依赖数据层
    RAGPipe --> EmbeddingEngine
    EmbeddingEngine --> ChromaStore
    RAGPipe --> SQLiteFTS
    PromptDynamic --> RedisCache
    SQLAlchemyORM --> MySQLDb

    %% Celery 任务调用与离线分析
    CeleryMQ --> RAGPipe
    CeleryMQ --> PromptDynamic

    %% 模型网关连接底层推理设施
    LangChainGate --> LocalInference
    LangChainGate --> CloudInference

    %% 统一应用纯黑白高清晰度样式
    class Vue3,Vite,Tailwind,Monaco,Katex,FastAPIApp,PyJWT,Jinja2,AsyncIO,SQLAlchemyORM,PromptDynamic,RAGPipe,EmbeddingEngine,LangChainGate,MySQLDb,RedisCache,ChromaStore,SQLiteFTS,CeleryMQ,LocalInference,CloudInference blackWhite;
```
### 2.4 运行环境设计

本系统的运行环境设计兼顾本地部署简单性与资源可扩展性，主要分为四层：

1. **客户端环境**：无需安装独立客户端，用户通过任意现代 Web 浏览器即可访问基于 Vue 3 + Vite 构建的前端 Web 应用，支持流式 Markdown、LaTeX 公式和代码渲染。
2. **后端容器化环境**：通过 Docker Compose 快速编排本地应用环境，包含基于 Python 3.10+ 的 FastAPI 异步后端框架、Celery Worker 任务执行器、Redis 8.0 消息队列与缓存，以及 MySQL 8.0 关系型数据库。
3. **存储与 RAG 解析环境**：依赖本地挂载卷实现数据持久化，嵌入 PyMuPDF4LLM 与 PaddleOCR 运行时解析 PDF 算法文档，并通过 ChromaDB 本地向量库与 SQLite FTS5 实现高性能混合检索。
4. **模型推理环境（灵活双模式）**：
  * **本地硬件推理模式**：宿主机搭载 NVIDIA GPU时，可通过 Ollama / vLLM 本地加载本地训练模型（可采用服务器端API）运行；
  * **轻量云端模式**：无独立显存设备时，仅依赖 Python 基础环境，通过网络直接接入外部云端模型 API 运行。

```mermaid
%%{init: { "flowchart": { "curve": "step"}}}%%
flowchart TD
    %% 纯黑白高对比度样式定义
    classDef blackWhite fill:#ffffff,stroke:#000000,stroke-width:2px,color:#000000;
    
    %% 设定所有连线和箭头的颜色为纯黑，线宽加粗至 2px
    linkStyle default stroke:#000000,stroke-width:2px;

    %% ==================== 1. 客户端运行环境 ====================
    subgraph ClientEnv ["1. 客户端运行环境 (Web Client)"]
        Browser["现代化 Web 浏览器<br>(Chrome / Edge / Safari / Firefox)"]
        FEStack["前端运行时环境<br>(Vue 3 + Vite + Tailwind CSS + Pinia)"]
    end

    %% ==================== 2. 本地服务器宿主环境 ====================
    subgraph ServerEnv ["2. 本地服务器 / 宿主主机环境 (Host OS)"]
        DockerHost["容器化/原生运行环境<br>(Ubuntu / Debian / macOS / Windows WSL2)"]
        
        subgraph AppRuntime ["后端与中间件容器集群 (Docker Compose)"]
            FastAPIContainer["FastAPI 后端服务<br>(Python 3.10+ / Uvicorn / Gunicorn)"]
            CeleryContainer["Celery 异步任务 Worker<br>(Python 3.10+ 环境)"]
            RedisContainer["Redis 服务容器<br>(消息队列 & Prompt 缓存)"]
            MySQLContainer["MySQL 8.0 容器<br>(主数据库 & 反馈日志)"]
        end
    end

    %% ==================== 3. 本地存储与检索底座 ====================
    subgraph StorageEnv ["3. 持久化存储与本地引擎环境"]
        EmbedEngine["文档解析与 OCR 引擎<br>(PyMuPDF4LLM + PaddleOCR)"]
        VectorStore["ChromaDB 本地向量库<br>(持久化挂载卷 Volume)"]
        FullTextStore["SQLite FTS5 全文索引<br>(本地轻量级数据库文件)"]
    end

    %% ==================== 4. 模型推理运行环境 ====================
    subgraph ModelEnv ["4. 大语言模型推理环境 (Model Gateway)"]
        subgraph LocalInference ["本地硬件 GPU 推理 (可选)"]
            OllamaEngine["Ollama / vLLM 推理引擎<br>(CUDA / MPS 硬件加速驱动)"]
            LocalWeights["本地训练大模型/已训练模型服务器API接口"]
        end
        
        subgraph CloudInference ["外部云端 API 访问 (可选)"]
            CloudAPIs["云端大模型 API 接口<br>(OpenAI / Claude / DeepSeek API)"]
        end
    end

    %% ==================== 基础链路与调用关系 ====================
    Browser --> FEStack
    FEStack -->|"HTTP / SSE 跨域通信 (Localhost/LAN)"| FastAPIContainer

    FastAPIContainer --> RedisContainer
    FastAPIContainer --> MySQLContainer
    FastAPIContainer --> CeleryContainer

    CeleryContainer --> EmbedEngine
    EmbedEngine --> VectorStore
    EmbedEngine --> FullTextStore

    FastAPIContainer -->|"API 请求转发"| OllamaEngine
    FastAPIContainer -->|"HTTPS 安全网络请求"| CloudAPIs
    OllamaEngine --> LocalWeights

    %% 统一应用纯黑白高清晰度样式
    class Browser,FEStack,DockerHost,FastAPIContainer,CeleryContainer,RedisContainer,MySQLContainer,EmbedEngine,VectorStore,FullTextStore,OllamaEngine,LocalWeights,CloudAPIs blackWhite;
```

###2.5 设计目标

本系统的核心设计目标在于构建一个**安全可控、具备自我进化能力的私有化 AI 教学与作业辅助平台**。系统在功能上聚焦于**人工智能经典算法的高精度 RAG 解析**与**启发式 Python 代码作业 Debug 引导**；在技术演进上，支持基于 **QLoRA 的本地开源基座模型自主微调**与 **GGUF 量化部署**，并无缝集成**Model Gateway 兼容云端 API**；在体验优化上，通过**Jinja2 动态引擎**实时融合用户个性化学习记忆，并利用 **FastAPI 切面 + Celery 离线分析**捕获纠错关键词，实现全局**System Prompt**的闭环自主迭代。
